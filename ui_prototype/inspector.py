# inspector.py: Debug/inspection tools for UI tree, state, etc.

class Inspector:
    def inspect_widget(self, widget):
        print(f"Inspecting widget: {widget}")

    def inspect_state(self, state):
        print(f"Inspecting state: {state}")
